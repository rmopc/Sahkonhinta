<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="com.companyname.s_hk_hinta_app" android:installLocation="auto">
	<uses-sdk android:minSdkVersion="31" android:targetSdkVersion="34" />
	<!-- The widget receiver is registered via [BroadcastReceiver]/[MetaData]
	     attributes on PriceWidgetProvider with a stable name, so it no longer
	     needs a hand-written entry with a generated crc class name here. -->
	<application android:label="SähköSpot.Android" android:theme="@style/MainTheme">
	</application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
